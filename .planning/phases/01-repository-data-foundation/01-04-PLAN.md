---
phase: 01-repository-data-foundation
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - src/etl/build_nodes.py
  - src/etl/run_pipeline.py
  - tests/test_etl_contracts.py
  - data/reference/validation_notes_mvp.md
  - README.md
autonomous: true
must_haves:
  truths:
    - "nodes.csv matches §6.3 for the same snapshot as edges."
    - "Automated check(s) validate column names and key relationships for the MVP slice."
    - "Validation notes document exclusions or join gaps (DATA-06); README points to them."
  artifacts:
    - path: "data/processed/{snapshot_id}/nodes.csv"
      provides: "§6.3 node metrics per airport"
    - path: "data/reference/validation_notes_mvp.md"
      provides: "DATA-06 dictionary / QA notes"
    - path: "tests/test_etl_contracts.py"
      provides: "Automated contract / join sanity"
  key_links:
    - from: "build_nodes.py"
      to: "edges.csv"
      via: "aggregates in/out degree and strength from analysis_weight"
      pattern: "groupby|edges"
---

<objective>
Complete **DATA-04**, **DATA-05**, and **DATA-06**: derive **nodes.csv**, provide a **single entrypoint** to regenerate processed tables, add **automated validation**, and publish **validation notes** for the MVP slice.

Purpose: Closes Phase 1 success criteria (reproducible path, contract checks, no silent broken joins).  
Output: `nodes.csv`, `src/etl/run_pipeline.py` (or `python -m src.etl.run_pipeline`), tests, `data/reference/validation_notes_mvp.md`, README ETL section.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@organization/ATNA_MVP_Technical_Spec_and_Workflow.md
@.planning/ROADMAP.md
@scripts/download/verify_downloads.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build nodes.csv from edges</name>
  <files>src/etl/build_nodes.py</files>
  <action>Read `edges.csv` for the snapshot; compute per-airport: flights_out, flights_in, strength_out (sum of analysis_weight out), strength_in (sum in), strength_total, degree_out/in/total as defined in §6.3 (degrees = counts of distinct edges or row counts per spec intent—document choice). Output `nodes.csv` with columns: snapshot_id, airport_id, flights_out, flights_in, strength_out, strength_in, strength_total, degree_out, degree_in, degree_total.</action>
  <verify>Assert column names match §6.3; strength_total ≈ strength_in + strength_out for consistency check.</verify>
  <done>nodes.csv written alongside airports and edges.</done>
</task>

<task type="auto">
  <name>Task 2: Pipeline entrypoint and README</name>
  <files>src/etl/run_pipeline.py, README.md</files>
  <action>Add `run_pipeline.py` that loads config, runs build_airports → build_edges → build_nodes in order, writing to `data/processed/{snapshot_id}/`. Accept optional `--config` path. Document exact command in README (`python -m src.etl.run_pipeline` or equivalent) after download + verify steps.</action>
  <verify>Running the command end-to-end completes without error when raw data exists for the snapshot (document prerequisite).</verify>
  <done>Documented one-command ETL from raw to three CSVs.</done>
</task>

<task type="auto">
  <name>Task 3: Contracts, join QA, DATA-06 notes</name>
  <files>tests/test_etl_contracts.py, data/reference/validation_notes_mvp.md</files>
  <action>Add pytest (or script invoked by pytest) that: asserts required columns for airports, edges, nodes; checks every `origin_id`/`destination_id` in edges exists in airports (or documents exceptions); reports row counts. Create `data/reference/validation_notes_mvp.md` listing snapshot_id, source files, exclusion counts, and any known BTS quirks. Link this file from README.</action>
  <verify>`pytest tests/test_etl_contracts.py -q` passes.</verify>
  <done>DATA-05 and DATA-06 satisfied; automated sanity path exists.</done>
</task>

</tasks>

<verification>
Full pipeline run + pytest green; validation notes exist and are linked.
</verification>

<success_criteria>
Phase 1 roadmap success criteria met: reproducible steps, contract checks, transparent join QA.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-data-foundation/01-04-SUMMARY.md`
</output>
