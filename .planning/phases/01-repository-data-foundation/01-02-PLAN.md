---
phase: 01-repository-data-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - config/atna.yaml
  - src/etl/config.py
  - README.md
autonomous: true
must_haves:
  truths:
    - "A single checked-in YAML defines canonical snapshot month and path roots for the pipeline."
    - "Executors can locate raw on-time, T-100, and master files for the chosen snapshot without env-only secrets."
  artifacts:
    - path: "config/atna.yaml"
      provides: "snapshot_id, raw subdirs, output prefix for processed tables"
    - path: "src/etl/config.py"
      provides: "Load and validate YAML; resolve paths from repo root"
  key_links:
    - from: "src/etl/config.py"
      to: "config/atna.yaml"
      via: "load on import or explicit load_config()"
      pattern: "atna.yaml"
---

<objective>
Add a **single pipeline configuration** (per `01-CONTEXT.md`) so ETL reads snapshot month and file locations from disk, not undocumented CLI/env alone.

Purpose: Unblocks deterministic rebuilds for one `YYYY-MM` MVP snapshot.  
Output: `config/atna.yaml`, small `src/etl/config.py` loader, README snippet pointing to the file.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-repository-data-foundation/01-CONTEXT.md
@.planning/phases/01-repository-data-foundation/01-RESEARCH.md
@data/reference/download_manifest_2025.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define config/atna.yaml</name>
  <files>config/atna.yaml</files>
  <action>Create `config/atna.yaml` with keys at minimum: `snapshot_id` (string `YYYY-MM`), `raw.on_time_glob` or path template, `raw.t100_glob`, `raw.master_airport_file`, `output.processed_dir` (e.g. `data/processed/{snapshot_id}/`). Values must align with actual repo paths after downloads (see `data/reference/` and README). Document the chosen MVP month in a comment in-file or adjacent one-line comment if YAML parser allows; otherwise document in README only in Task 2.</action>
  <verify>File parses with PyYAML: `python -c "import yaml; yaml.safe_load(open('config/atna.yaml'))"` from repo root.</verify>
  <done>Valid YAML; paths are plausible for this repo.</done>
</task>

<task type="auto">
  <name>Task 2: Config loader module</name>
  <files>src/etl/config.py</files>
  <action>Implement `load_config(path=None)` returning a typed structure or dict with resolved `Path` objects relative to repo root (`REPO_ROOT` pattern like existing scripts). Validate `snapshot_id` matches `^\d{4}-\d{2}$`. Raise clear errors for missing files when checked in optional `validate_paths()` for development.</action>
  <verify>From repo root, config loads: e.g. `python -c "import sys; from pathlib import Path; sys.path.insert(0, str(Path('src').resolve())); from etl.config import load_config; load_config()"` OR `pytest` with `pythonpath=src` in `pyproject.toml`/`pytest.ini` — choose one approach and document it in README.</verify>
  <done>Config loads without error from repo root.</done>
</task>

<task type="auto">
  <name>Task 3: Document configuration in README</name>
  <files>README.md</files>
  <action>Add subsection: where `config/atna.yaml` lives, what to edit to bump MVP snapshot, and that download must be complete for listed paths. Cross-link `verify_downloads.py`.</action>
  <verify>README contains `config/atna.yaml` and snapshot bump instructions.</verify>
  <done>Configuration discoverable from README.</done>
</task>

</tasks>

<verification>
Config round-trips through YAML and Python; snapshot_id validated.
</verification>

<success_criteria>
Single YAML drives paths; loader tested or import-smoke-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-data-foundation/01-02-SUMMARY.md`
</output>
