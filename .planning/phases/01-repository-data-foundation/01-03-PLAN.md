---
phase: 01-repository-data-foundation
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/etl/load_raw.py
  - src/etl/build_airports.py
  - src/etl/build_edges.py
  - src/etl/__init__.py
autonomous: true
must_haves:
  truths:
    - "airports.csv matches §6.1 required column names and one row per airport in the MVP slice."
    - "edges.csv matches §6.2 including snapshot_id, flight_count aggregates, analysis_weight = log(1 + flight_count), and route_key format."
  artifacts:
    - path: "data/processed/{snapshot_id}/airports.csv"
      provides: "§6.1 canonical airport table"
    - path: "data/processed/{snapshot_id}/edges.csv"
      provides: "§6.2 directed route edges for snapshot"
  key_links:
    - from: "build_edges.py"
      to: "load_raw.py"
      via: "loads filtered on-time (+ T100 joins as needed for pax/seat columns)"
      pattern: "flight_count|groupby"
---

<objective>
Implement **DATA-02** and **DATA-03**: load raw BTS CSVs for the configured snapshot, build **airports.csv** and **edges.csv** per spec §6.1–6.2.

Purpose: Core ETL for the graph tables feeding Phase 2.  
Output: Written CSVs under `data/processed/{snapshot_id}/` with required columns populated from master + on-time (+ T-100 where columns require).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@organization/ATNA_MVP_Technical_Spec_and_Workflow.md
@data/reference/field_selection_notes.md
@.planning/phases/01-repository-data-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raw loaders filtered to snapshot</name>
  <files>src/etl/load_raw.py</files>
  <action>Implement functions to read master airport file, monthly on-time CSV, and T-100 CSV for `snapshot_id` from config. Filter rows to `YEAR`/`MONTH` matching snapshot. Use dtypes that preserve IDs (string or Int64). Document in module docstring which raw columns map to spec outputs. Follow `field_selection_notes.md` for ID vs IATA usage.</action>
  <verify>Run a small script or pytest that loads raw data for the configured snapshot and asserts non-empty frames where files exist.</verify>
  <done>Loaders return filtered DataFrames for the MVP month.</done>
</task>

<task type="auto">
  <name>Task 2: Build airports.csv</name>
  <files>src/etl/build_airports.py</files>
  <action>From master (and any needed joins), emit airports present in the MVP slice with columns: airport_id_canonical, airport_code_raw, airport_name, city, state, country, latitude, longitude, timezone, active_flag, source_version. Map BTS fields to these names per §6.1; use consistent ID as `airport_id_canonical`. Write `airports.csv` to config output dir.</action>
  <verify>Script or test asserts column set equals §6.1 list (order may match spec); no duplicate airport_id_canonical.</verify>
  <done>airports.csv written with required headers and sane row count.</done>
</task>

<task type="auto">
  <name>Task 3: Build edges.csv</name>
  <files>src/etl/build_edges.py</files>
  <action>Aggregate on-time (and T-100 for passenger/seat fields) to one row per directed origin_id/destination_id for the snapshot. Required columns per §6.2: snapshot_id, year, month, origin_id, destination_id, flight_count, passenger_count, seat_count, avg_arr_delay, pct_delayed, analysis_weight, route_key. Compute flight_count from operations (e.g. row counts or sum of flights field per route). Set `analysis_weight = numpy.log1p(flight_count)`. Set `route_key = f\"{origin_id}__{destination_id}__{snapshot_id}\"`. Write `edges.csv`.</action>
  <verify>Assert `analysis_weight` equals `log1p(flight_count)` within tolerance; assert `route_key` pattern; column list matches §6.2.</verify>
  <done>edges.csv written; spot-check one route manually from raw if needed.</done>
</task>

</tasks>

<verification>
Column contracts for §6.1 and §6.2; analysis_weight formula verified in code or test.
</verification>

<success_criteria>
DATA-02 and DATA-03 satisfied for one snapshot; outputs on disk under processed dir.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-data-foundation/01-03-SUMMARY.md`
</output>
