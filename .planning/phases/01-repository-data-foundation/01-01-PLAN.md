---
phase: 01-repository-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - requirements.txt
  - docs/specs/README.md
  - src/etl/__init__.py
  - data/interim/.gitkeep
  - data/processed/.gitkeep
  - outputs/tables/.gitkeep
  - notebooks/exploration/.gitkeep
  - tests/__init__.py
autonomous: true
must_haves:
  truths:
    - "Repository tree matches spec §4.2 boundaries (data/, outputs/, src/, tests/, requirements.txt, README)."
    - "README states that the locked organization spec is the schema contract (REPO-02)."
    - "Raw data policy (data/raw/ immutability, no silent overwrites) is documented and discoverable."
  artifacts:
    - path: "src/etl/__init__.py"
      provides: "ETL package root"
    - path: "docs/specs/README.md"
      provides: "Pointer to locked MVP spec path and role"
    - path: "requirements.txt"
      provides: "Pinned Python deps including pandas, PyYAML, pytest"
  key_links:
    - from: "README.md"
      to: "organization/ATNA_MVP_Technical_Spec_and_Workflow.md"
      via: "Explicit link or path reference as contract"
      pattern: "organization/ATNA"
---

<objective>
Establish the repository skeleton, Python dependencies, and governance narrative so Phase 1 work aligns with **REPO-01**, **REPO-02**, and **DATA-01** documentation (raw layout already partially populated by download scripts).

Purpose: Downstream plans assume stable `src/etl/`, processed output locations, and a single source of truth for schemas.  
Output: Directory layout, updated README, `requirements.txt`, and `docs/specs` pointer to the locked spec.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repository-data-foundation/01-CONTEXT.md
@.planning/phases/01-repository-data-foundation/01-RESEARCH.md
@organization/ATNA_MVP_Technical_Spec_and_Workflow.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create §4.2 directory skeleton</name>
  <files>src/etl/__init__.py, data/interim/.gitkeep, data/processed/.gitkeep, outputs/tables/.gitkeep, notebooks/exploration/.gitkeep, tests/__init__.py, docs/specs/README.md</files>
  <action>Create missing folders per spec §4.2: `src/etl/` (package), `data/interim/`, `data/processed/`, `outputs/tables/`, `notebooks/exploration/`, `tests/`, `docs/specs/`. Add minimal `__init__.py` / `.gitkeep` so empty dirs are tracked. Do not relocate existing `data/raw/` or `scripts/` content. Add `docs/specs/README.md` that states the canonical spec file is `organization/ATNA_MVP_Technical_Spec_and_Workflow.md` and that it governs §6 column contracts (REPO-02).</action>
  <verify>From repo root: `Test-Path src/etl, data/processed, outputs/tables, notebooks/exploration, tests, docs/specs` (PowerShell) or equivalent; confirm `docs/specs/README.md` contains the organization spec path.</verify>
  <done>Layout exists; spec pointer file created.</done>
</task>

<task type="auto">
  <name>Task 2: Dependencies and test runner</name>
  <files>requirements.txt</files>
  <action>Ensure `requirements.txt` includes pinned or lower-bounded versions for **pandas**, **PyYAML**, **pytest**, and **numpy** (if not pulled transitively). Include any deps already required by `scripts/download/download_bts_data.py` (e.g. playwright) if not already listed—do not remove existing entries. Keep file valid for `pip install -r requirements.txt`.</action>
  <verify>`pip install -r requirements.txt` succeeds in a clean venv (or dry-run check syntax).</verify>
  <done>requirements.txt installs without error.</done>
</task>

<task type="auto">
  <name>Task 3: README — reproducibility and DATA-01</name>
  <files>README.md</files>
  <action>Extend README: (1) document target repo layout referencing §4.2; (2) state that **raw** inputs live under `data/raw/` and must not be silently overwritten—re-fetch uses new dated paths or suffixes per `01-CONTEXT.md`; (3) link to `docs/specs/README.md` and locked spec; (4) separate bullets for **download** (existing script) vs **ETL** (upcoming) steps. Keep existing download section intact; add above or below clearly.</action>
  <verify>Manual read: README mentions immutability, spec link, download vs ETL separation.</verify>
  <done>README reflects REPO-02 and DATA-01 policy in prose.</done>
</task>

</tasks>

<verification>
- Spec path referenced from `docs/specs/README.md` and README.
- `src/etl` and output dirs exist for later plans.
</verification>

<success_criteria>
REPO-01 layout present; REPO-02 documented; DATA-01 raw policy documented.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-data-foundation/01-01-SUMMARY.md`
</output>
