---
phase: 05-qa-demo-polish-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data/reference/validation_checklist_phase5.md
autonomous: true
must_haves:
  truths:
    - "Every §13 checklist bullet (13.1–13.4) has a recorded outcome and an evidence pointer or explicit waiver."
    - "Automatable checks are tied to pytest or existing scripts where possible; subjective items have human sign-off."
    - "Gaps that are spec bugs vs scope creep are labeled per spec §16.7 QA prompt intent."
  artifacts:
    - path: "data/reference/validation_checklist_phase5.md"
      provides: "matrix of spec §13 themes × bullets with pass/fail/waived + evidence"
  key_links:
    - from: "data/reference/validation_checklist_phase5.md"
      to: "organization/ATNA_MVP_Technical_Spec_and_Workflow.md"
      via: "section 13 row headers"
      pattern: "§13"
---

<objective>
Close **QA-01**: execute validation themes from spec **§13** (data, metrics, scenario, UI) and capture outcomes in a single durable checklist artifact under `data/reference/`.

Purpose: prove MVP integration against the locked checklist without inventing new requirements.
Output: completed `validation_checklist_phase5.md` plus any minimal fixes only if a failing item is a true defect (otherwise record as known limitation with rationale).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-qa-demo-polish-documentation/05-RESEARCH.md
@organization/ATNA_MVP_Technical_Spec_and_Workflow.md
@data/reference/validation_notes_mvp.md
@data/reference/validation_notes_phase2.md
@.planning/phases/04-streamlit-application/04-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold §13 checklist document</name>
  <files>data/reference/validation_checklist_phase5.md</files>
  <action>Create `data/reference/validation_checklist_phase5.md` with four subsections mirroring spec §13.1–§13.4. For each spec bullet, add columns or rows for: **Outcome** (Pass / Fail / Waived), **Evidence** (test module, file path, log, or “Manual”), **Notes** (one line). Include snapshot_id from `config/atna.yaml` in a header block.</action>
  <verify>File exists and contains at least every bullet from spec §13 verbatim or clearly quoted.</verify>
  <done>Structured template ready to fill; no empty sections.</done>
</task>

<task type="auto">
  <name>Task 2: Fill data theme (§13.1) with evidence</name>
  <files>data/reference/validation_checklist_phase5.md</files>
  <action>Map each §13.1 bullet to existing validation: joins, coordinates, aggregation, snapshot consistency. Reference `validation_notes_mvp.md`, `tests/test_etl_contracts.py`, and `scripts/download/verify_downloads.py` as appropriate. Run the relevant pytest subset and record command + result summary in the checklist.</action>
  <verify>`pytest tests/test_etl_contracts.py tests/test_etl_01_03.py -q` (or documented subset) passes; checklist rows updated.</verify>
  <done>§13.1 rows are Pass with pointers or Fail with filed follow-up in Notes.</done>
</task>

<task type="auto">
  <name>Task 3: Fill metrics theme (§13.2)</name>
  <files>data/reference/validation_checklist_phase5.md</files>
  <action>Link hub/bridge distinction, community outputs, and sanity to tests and existing phase-2 notes/maps. Run metric/community/route tests that enforce contracts; add a **Human** row for “intuitive hubs” and “spatially interpretable communities” with a signed one-line assessment (name/date optional per team norm).</action>
  <verify>Documented pytest command for metrics-related tests passes; human rows explicitly marked.</verify>
  <done>§13.2 complete with mixed automated + human evidence.</done>
</task>

<task type="auto">
  <name>Task 4: Fill scenario theme (§13.3)</name>
  <files>data/reference/validation_checklist_phase5.md</files>
  <action>Use ripple/scoring/engine tests to cover hop weakening, impact vs network health direction, and graph-path validity. Note any UI-only confirmation deferred to manual demo (cross-reference §14 rehearsal in plan 05-02).</action>
  <verify>`pytest tests/test_scenario_ripple_scoring.py tests/test_scenario_engine_artifacts.py tests/test_vulnerability_metrics_integration.py -q` passes.</verify>
  <done>§13.3 rows tied to tests; UI scenario editor confirmation either Pass with manual note or explicit follow-up.</done>
</task>

<task type="auto">
  <name>Task 5: Fill UI theme (§13.4)</name>
  <files>data/reference/validation_checklist_phase5.md</files>
  <action>Run `pytest tests/test_streamlit_app_smoke.py -q`. For methodology vs formulas, spot-check `src/app/pages/methodology.py` against spec formulas already locked in STATE decisions; record Pass/Fail. Manual rows: filters/charts, metadata correctness — brief human confirmation from a single walkthrough or cite `04-VERIFICATION.md` if still accurate.</action>
  <verify>AppTest smoke passes; methodology spot-check recorded.</verify>
  <done>§13.4 complete.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_etl_contracts.py tests/test_etl_01_03.py -q`
- `pytest tests/test_streamlit_app_smoke.py -q`
- `pytest tests/test_scenario_ripple_scoring.py tests/test_scenario_engine_artifacts.py tests/test_vulnerability_metrics_integration.py -q`
- Confirm `data/reference/validation_checklist_phase5.md` has no blank Outcome for any §13 bullet.
</verification>

<success_criteria>
- QA-01 satisfied: §13 executed with recorded outcomes.
- One canonical checklist file under `data/reference/` is the source of truth for Phase 5 QA sign-off.
</success_criteria>

<output>
After completion, create `.planning/phases/05-qa-demo-polish-documentation/05-01-SUMMARY.md`
</output>
