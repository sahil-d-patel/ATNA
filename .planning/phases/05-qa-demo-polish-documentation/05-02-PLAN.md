---
phase: 05-qa-demo-polish-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - README.md
autonomous: true
must_haves:
  truths:
    - "README documents environment, ETL, metrics build, scenario batch run, and Streamlit launch with commands that match the repo layout."
    - "Spec §14 demo sequence appears as a numbered rehearsal list a presenter can follow without opening the spec."
    - "Commands use `config/atna.yaml` snapshot_id semantics; Windows and Unix examples both present where they differ."
  artifacts:
    - path: "README.md"
      provides: "end-to-end reproduce + demo narrative for QA-02"
  key_links:
    - from: "README.md"
      to: "config/atna.yaml"
      via: "snapshot_id and pipeline order"
      pattern: "snapshot_id|PYTHONPATH|streamlit"
---

<objective>
Close **QA-02**: **stable demo build** — README is the single entrypoint for reproducing pipeline outputs and launching the app; include spec **§14** demo sequence as a rehearsal script.

Purpose: external reviewers and demo audience can clone, install, build artifacts, and run the Streamlit app without tribal knowledge.
Output: updated `README.md` only (no new top-level doc files unless strictly necessary).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-qa-demo-polish-documentation/05-RESEARCH.md
@README.md
@config/atna.yaml
@src/metrics/run_metrics.py
@src/scenarios/run_scenarios.py
@src/app/streamlit_app.py
@organization/ATNA_MVP_Technical_Spec_and_Workflow.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add “Metrics & scenarios build” section to README</name>
  <files>README.md</files>
  <action>After the ETL section, add a concise section ordering: (1) ETL already done, (2) `PYTHONPATH=src python -m metrics.run_metrics` prerequisites (processed `edges.csv`/`nodes.csv` exist), (3) `PYTHONPATH=src python -m scenarios.run_scenarios` for demo scenario artifacts. Mirror PowerShell vs bash style already used in README. Mention output paths under `data/processed/{snapshot_id}/` aligned with `config/atna.yaml`.</action>
  <verify>From a clean venv, a reader can copy commands in order; no undefined module paths.</verify>
  <done>README contains metrics and scenario commands with correct module names.</done>
</task>

<task type="auto">
  <name>Task 2: Add “Run the Streamlit app” section</name>
  <files>README.md</files>
  <action>Document `streamlit run src/app/streamlit_app.py` from repository root, dependency note (`pip install -r requirements.txt`), and that artifacts for `snapshot_id` must exist. Cross-link to `data/reference/validation_checklist_phase5.md` and existing validation notes.</action>
  <verify>Optional smoke: `streamlit run src/app/streamlit_app.py --help` or document that headless CI uses `pytest tests/test_streamlit_app_smoke.py`.</verify>
  <done>Launch instructions are unambiguous for local MVP.</done>
</task>

<task type="auto">
  <name>Task 3: Embed §14 demo sequence</name>
  <files>README.md</files>
  <action>Add subsection **Demo sequence (spec §14)** with numbered steps 1–10 matching the locked spec (snapshot → map → explorer rankings → communities → route explorer → scenario removals → takeaway). Keep wording presentation-focused; do not alter spec meaning.</action>
  <verify>Step count and order match spec §14; no extra MVP features implied.</verify>
  <done>Presenters can rehearse from README alone.</done>
</task>

<task type="human">
  <name>Task 4: Rehearse demo once and fix README friction only</name>
  <files>README.md</files>
  <action>Execute the README path end-to-end on one machine (fresh or documented venv): install deps, verify downloads if needed, ETL, metrics, scenarios, app launch, then walk §14 steps. If any step fails due to doc gap, patch README; if due to product bug, note in checklist (05-01) or file minimal fix if trivial.</action>
  <verify>Operator sign-off line in README footer or in `05-02-SUMMARY.md` (date + pass).</verify>
  <done>Demo narrative is rehearsed; README updated for any friction found.</done>
</task>

</tasks>

<verification>
- Grep README for `metrics.run_metrics`, `scenarios.run_scenarios`, and `streamlit run src/app/streamlit_app.py`.
- Manually confirm §14 step count equals ten and mirrors spec wording.
</verification>

<success_criteria>
- QA-02 satisfied: README is sufficient to reproduce pipeline and run the app.
- §14 demo is documented for rehearsal.
</success_criteria>

<output>
After completion, create `.planning/phases/05-qa-demo-polish-documentation/05-02-SUMMARY.md`
</output>
