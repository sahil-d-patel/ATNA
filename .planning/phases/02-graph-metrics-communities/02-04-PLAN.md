---
phase: 02-graph-metrics-communities
plan: 04
type: execute
wave: 4
depends_on: ["03"]
files_modified:
  - src/metrics/leiden_communities.py
  - src/metrics/run_metrics.py
  - tests/test_communities.py
autonomous: true
must_haves:
  truths:
    - "Leiden partition assigns each airport a leiden_community_id; ids align between metrics.csv and communities.csv."
    - "communities.csv satisfies §6.5 including community_traffic, internal_density, top hub/bridge airport lists."
    - "Community traffic and density formulas follow §7.9."
  artifacts:
    - path: "data/processed/{snapshot_id}/communities.csv"
      provides: "Per-community rollup"
    - path: "data/processed/{snapshot_id}/metrics.csv"
      provides: "Updated leiden_community_id column"
  key_links:
    - from: "leiden_communities.py"
      to: "graph_builder.py"
      via: "NetworkX → igraph conversion + leidenalg"
---

<objective>
Implement **METR-05**: **Leiden** clustering, **`communities.csv`**, and **`leiden_community_id`** on **`metrics.csv`** per §6.5 and §7.9.

Purpose: Unlocks cross-community route logic and Phase 4 Communities page.  
Output: `leiden_communities.py`, updated pipeline step, tests, both CSVs updated/written.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@organization/ATNA_MVP_Technical_Spec_and_Workflow.md
@.planning/phases/02-graph-metrics-communities/02-RESEARCH.md
@src/metrics/graph_builder.py
@src/metrics/hub_bridge.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Leiden partition</name>
  <files>src/metrics/leiden_communities.py</files>
  <action>Convert the **same** NetworkX DiGraph to **igraph** (directed, weights on analysis weight). Run **Leiden** (document resolution/weights parameter defaults; seed if library supports for basic repeatability). Produce mapping **airport_id → leiden_community_id** (integer labels 0..K-1 or stable sorted ids). Handle disconnected structure per RESEARCH open question with a one-line documented choice.</action>
  <verify>Smoke test: partition covers all graph nodes exactly once; module imports with igraph+leidenalg installed.</verify>
  <done>Leiden labels available in memory.</done>
</task>

<task type="auto">
  <name>Task 2: communities.csv metrics</name>
  <files>src/metrics/leiden_communities.py</files>
  <action>For each community: **community_size**, **community_traffic** = Σ w(i,j) over i,j in C (§7.9), **internal_density** = internal_edges / (|C|(|C|-1)) with correct handling of |C|<2; **top_hub_airport_ids** and **top_bridge_airport_ids** (pick top N per CONTEXT, e.g. 5; serialize IDs with a **documented delimiter**). Output one row per community per **snapshot_id**.</action>
  <verify>pytest: toy partition yields expected traffic sum on 2-node internal edge.</verify>
  <done>§6.5 row shape validated.</done>
</task>

<task type="auto">
  <name>Task 3: Merge into metrics.csv and integration test</name>
  <files>src/metrics/run_metrics.py, tests/test_communities.py</files>
  <action>Extend `run_metrics` to write **`communities.csv`** and **overwrite/update** `metrics.csv` **leiden_community_id** (replace placeholder). Test: every **airport_id** in metrics matches a partition; community ids referenced in communities.csv match unique set in metrics.</action>
  <verify>`pytest tests/test_communities.py -q` passes; optional full run on MVP snapshot produces non-empty communities.csv.</verify>
  <done>METR-05 complete.</done>
</task>

</tasks>

<verification>
pytest; CSV round-trip id consistency.
</verification>

<success_criteria>
Leiden artifacts match spec; metrics and communities synchronized on community IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/02-graph-metrics-communities/02-04-SUMMARY.md`
</output>
