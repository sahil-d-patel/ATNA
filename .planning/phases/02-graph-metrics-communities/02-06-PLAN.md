---
phase: 02-graph-metrics-communities
plan: 06
type: execute
wave: 6
depends_on: ["05"]
files_modified:
  - scripts/metrics/community_map_check.py
  - data/reference/validation_notes_phase2.md
autonomous: false
must_haves:
  truths:
    - "A static map or notebook renders airports colored by Leiden community with size scaled by strength or traffic; singleton/tiny communities are de-emphasized per 02-CONTEXT."
    - "Phase 2 validation notes record Python version, key package versions, and balanced hub/bridge/PageRank sanity narrative."
    - "A human confirms the geographic smoke test (coherent patches, no single-color dominance bug)."
  artifacts:
    - path: "scripts/metrics/community_map_check.py"
      provides: "Map smoke artifact (matplotlib or folium — implementer choice)"
    - path: "data/reference/validation_notes_phase2.md"
      provides: "METR QA narrative + versions"
  key_links:
    - from: "community_map_check.py"
      to: "metrics.csv + airports.csv"
      via: "merge lat/lon for scatter/plot"
---

<objective>
Satisfy roadmap **Phase 2 success criterion 2** (community map legibility) and **criterion 1** documentation (sanity narrative); align with **02-CONTEXT** for visual QA and validation-notes-only interpretation copy.

Purpose: Catches Leiden/geo bugs before Streamlit.  
Output: runnable map script, `validation_notes_phase2.md`, human verification checkpoint.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-graph-metrics-communities/02-CONTEXT.md
@data/processed
@data/reference
</context>

<tasks>

<task type="auto">
  <name>Task 1: Community map smoke script</name>
  <files>scripts/metrics/community_map_check.py</files>
  <action>Load **airports.csv** (lat/lon), **metrics.csv** (leiden_community_id, strength_total or hub_score for size). Plot **lower 48 + AK/HI** as in CONTEXT (filter by coordinates or state if available). **Distinct color per community**; **de-emphasize** small communities (alpha/size threshold per CONTEXT). Write output to **`outputs/maps/`** or **`outputs/phase2_community_map.png`** (create dir if needed). Add `if __name__ == "__main__"` entrypoint with argparse `--snapshot` or config path.</action>
  <verify>Running the script completes without exception when processed artifacts exist; PNG (or HTML) file created.</verify>
  <done>Automated map artifact for reviewer.</done>
</task>

<task type="auto">
  <name>Task 2: validation_notes_phase2.md</name>
  <files>data/reference/validation_notes_phase2.md</files>
  <action>Create notes: **date**, **snapshot_id**, **Python version**, **networkx / igraph / leidenalg / pandas / numpy** versions; short **hub vs bridge vs PageRank** sanity paragraph (equal emphasis per CONTEXT); **route criticality** spot-check bullets (1–2 structural examples if found); **METR-02** reconciliation outcome; **reproducibility** stance (seeds, approximate vs exact). State **vulnerability_score** remains NaN until Phase 3.</action>
  <verify>File exists and is linked from README under a short “Phase 2 metrics” bullet OR from main validation notes index if present.</verify>
  <done>DATA-style QA trail for metrics phase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Static community map (`outputs/...`) plus validation notes.</what-built>
  <how-to-verify>
    1. Open the generated map image (or HTML).
    2. Confirm **multiple** community colors visible; **large** communities read as geographic patches; **no obvious bug** where one color dominates the entire map incorrectly.
    3. Skim `data/reference/validation_notes_phase2.md` for version table and hub/bridge/PageRank balance.
  </how-to-verify>
  <resume-signal>Type **approved** or list specific visual/data issues to fix before Phase 2 verification.</resume-signal>
</task>

</tasks>

<verification>
Script runs; notes on disk; human approval recorded in chat or SUMMARY.
</verification>

<success_criteria>
Roadmap Phase 2 map + documentation exit criteria addressed.
</success_criteria>

<output>
After completion, create `.planning/phases/02-graph-metrics-communities/02-06-SUMMARY.md`
</output>
