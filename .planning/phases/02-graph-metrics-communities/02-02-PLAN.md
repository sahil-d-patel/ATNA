---
phase: 02-graph-metrics-communities
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/metrics/percentile.py
  - src/metrics/centralities.py
  - tests/test_centralities.py
autonomous: true
must_haves:
  truths:
    - "PageRank and betweenness are computed on the directed weighted analysis graph (§7.4)."
    - "P(·) percentile ranks map each metric to 0–100 across airports in the snapshot (§7.5)."
    - "Eigenvector is returned when numerically stable; otherwise NaN with no crash (§7.4 secondary)."
  artifacts:
    - path: "src/metrics/percentile.py"
      provides: "P(metric) scaling"
    - path: "src/metrics/centralities.py"
      provides: "pagerank, betweenness, eigenvector"
  key_links:
    - from: "centralities.py"
      to: "graph_builder.py"
      via: "accepts nx.DiGraph from builder"
---

<objective>
Implement **METR-03** (PageRank, betweenness, eigenvector) and the **§7.5 percentile helper** used by hub/bridge and route scores later.

Purpose: Centralities must be reproducible from the graph alone and comparable via P(·).  
Output: `percentile.py`, `centralities.py`, tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@organization/ATNA_MVP_Technical_Spec_and_Workflow.md
@.planning/phases/02-graph-metrics-communities/02-RESEARCH.md
@src/metrics/graph_builder.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Percentile scaling P(·)</name>
  <files>src/metrics/percentile.py</files>
  <action>Implement `percentile_rank_0_100(series: pd.Series) -> pd.Series` using **percentile rank** scaled to **0–100** (pandas `rank(pct=True) * 100` or equivalent). Document tie-breaking. Apply to **all airports in the snapshot** (include zeros where applicable).</action>
  <verify>Unit test: monotonic input yields non-decreasing P; known small set matches hand-ranked percentiles within tie rules.</verify>
  <done>§7.5 helper available for hub/bridge and route criticality.</done>
</task>

<task type="auto">
  <name>Task 2: Centrality computation</name>
  <files>src/metrics/centralities.py</files>
  <action>Functions taking `G: nx.DiGraph` and returning `pd.Series` (indexed by airport_id): **PageRank** with `weight='weight'`; **betweenness** with `weight='weight'` (or documented NetworkX parameter alignment for weighted directed bridge analysis per §7.4); **eigenvector** with try/except — on failure return all-NaN series with log in debug only.</action>
  <verify>On a tiny 3-node toy digraph, PageRank sums approximately normalize; betweenness finite; eigenvector either finite or NaN without exception.</verify>
  <done>All three metrics computable for smoke graphs.</done>
</task>

<task type="auto">
  <name>Task 3: Tests and optional snapshot smoke</name>
  <files>tests/test_centralities.py</files>
  <action>Test percentile + centralities modules. Optional: load real graph from plan 01 builder and assert no NaN for PageRank/betweenness on MVP data; eigenvector allowed NaN.</action>
  <verify>`pytest tests/test_centralities.py -q` passes.</verify>
  <done>METR-03 covered by automated checks.</done>
</task>

</tasks>

<verification>
pytest green; real-data smoke optional but logged in SUMMARY if run.
</verification>

<success_criteria>
METR-03 and §7.5 foundation implemented and tested.
</success_criteria>

<output>
After completion, create `.planning/phases/02-graph-metrics-communities/02-02-SUMMARY.md`
</output>
