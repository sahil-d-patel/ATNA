---
phase: 02-graph-metrics-communities
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - config/atna.yaml
  - src/metrics/__init__.py
  - src/metrics/config.py
  - src/metrics/graph_builder.py
  - tests/test_metrics_graph.py
autonomous: true
must_haves:
  truths:
    - "A directed weighted graph is built from processed edges using analysis_weight only (§7.2)."
    - "Automated tests prove graph order/size consistency with edges.csv for the configured snapshot."
  artifacts:
    - path: "src/metrics/graph_builder.py"
      provides: "METR-01 DiGraph construction"
    - path: "tests/test_metrics_graph.py"
      provides: "Graph contract tests"
  key_links:
    - from: "graph_builder.py"
      to: "data/processed/{snapshot_id}/edges.csv"
      via: "pandas read + nx.add_edge(..., weight=analysis_weight)"
---

<objective>
Establish the **metrics package**, **dependencies**, **config paths** for metric artifacts, and **METR-01**: build a **NetworkX `DiGraph`** from `edges.csv` using **`analysis_weight`**.

Purpose: Everything downstream assumes one canonical graph definition.  
Output: `src/metrics/*` skeleton, updated `requirements.txt` and `config/atna.yaml`, passing graph tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@organization/ATNA_MVP_Technical_Spec_and_Workflow.md
@.planning/phases/02-graph-metrics-communities/02-RESEARCH.md
@.planning/phases/02-graph-metrics-communities/02-CONTEXT.md
@src/etl/config.py
@config/atna.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependencies and config hooks</name>
  <files>requirements.txt, config/atna.yaml, src/metrics/config.py, src/metrics/__init__.py</files>
  <action>Add **networkx**, **python-igraph**, and **leidenalg** to `requirements.txt` with compatible bounds (see RESEARCH.md). Extend `atna.yaml` `output` (or equivalent) with explicit paths for `metrics.csv`, `communities.csv`, and `route_metrics.csv` under `data/processed/{snapshot_id}/`, resolved from repo root. Implement `src/metrics/config.py` mirroring `src/etl/config.py` patterns (load YAML, resolve `snapshot_id`, template paths).</action>
  <verify>`python -c "from src.metrics.config import load_config; load_config()"` succeeds from repo root; `pip install -r requirements.txt` (or uv/pip) completes without conflict.</verify>
  <done>Deps declared; metrics config loads and points at processed snapshot directory.</done>
</task>

<task type="auto">
  <name>Task 2: Graph builder (METR-01)</name>
  <files>src/metrics/graph_builder.py</files>
  <action>Implement `load_edges(snapshot_id|paths)` and `build_analysis_graph(edges_df) -> nx.DiGraph`: edges are **(origin_id, destination_id)** with **`weight = analysis_weight`**. Do **not** use `flight_count` as analysis weight. Include `snapshot_id` filter if loading from full dataframe. Document multi-edge policy (aggregate vs assert single row per directed pair — match `edges.csv` invariant from Phase 1).</action>
  <verify>Import and build graph on real `data/processed/{snapshot}/edges.csv` when present; assert edge count matches dataframe row count (or documented aggregate rule).</verify>
  <done>METR-01 satisfied for MVP snapshot.</done>
</task>

<task type="auto">
  <name>Task 3: Pytest graph contracts</name>
  <files>tests/test_metrics_graph.py</files>
  <action>Add tests: small synthetic edges table → known node/edge counts and one manual weight sum; optional skip if processed edges missing in CI. Fail if any edge weight ≤ 0 where spec expects log1p-positive counts.</action>
  <verify>`pytest tests/test_metrics_graph.py -q` passes.</verify>
  <done>Automated guardrails for graph construction.</done>
</task>

</tasks>

<verification>
Config load + pytest green; graph builds from real edges when data present.
</verification>

<success_criteria>
METR-01 complete: directed weighted analysis graph from processed edges alone.
</success_criteria>

<output>
After completion, create `.planning/phases/02-graph-metrics-communities/02-01-SUMMARY.md`
</output>
