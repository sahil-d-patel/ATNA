---
phase: 02-graph-metrics-communities
plan: 05
type: execute
wave: 5
depends_on: ["04"]
files_modified:
  - src/metrics/route_criticality.py
  - src/metrics/run_metrics.py
  - tests/test_route_metrics.py
autonomous: true
must_haves:
  truths:
    - "route_metrics.csv has §6.6 columns including cross_community_flag and route_criticality_score."
    - "RouteCriticality uses §7.10: 0.70*P(w(i,j)) + 0.30*CrossCommunity with CrossCommunity ∈ {0,100}."
    - "Cross-community route count is neither zero nor 100% for MVP snapshot (sanity band documented if violated)."
  artifacts:
    - path: "data/processed/{snapshot_id}/route_metrics.csv"
      provides: "Per-route structural scores"
    - path: "src/metrics/route_criticality.py"
      provides: "Criticality + flags"
  key_links:
    - from: "route_criticality.py"
      to: "metrics.csv / partition"
      via: "origin/dest community ids for cross_community_flag"
---

<objective>
Implement **METR-06**: **`route_metrics.csv`** with **route criticality** and **cross-community** flag per §6.6 and §7.10.

Purpose: Feeds Route explorer and scenario thinking in later phases.  
Output: `route_criticality.py`, pipeline integration, tests, CSV.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@organization/ATNA_MVP_Technical_Spec_and_Workflow.md
@src/metrics/percentile.py
@src/metrics/graph_builder.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-community flag and weights</name>
  <files>src/metrics/route_criticality.py</files>
  <action>Iterate **edges.csv** rows (or graph edges): for each directed pair, set **cross_community_flag** = 100 if origin and destination **leiden_community_id** differ, else 0 (§7.10). Preserve **analysis_weight** on output. Join community labels from finalized **metrics.csv** or in-memory partition.</action>
  <verify>Unit test: two airports same community → 0; different → 100.</verify>
  <done>Flag logic correct.</done>
</task>

<task type="auto">
  <name>Task 2: Route criticality score</name>
  <files>src/metrics/route_criticality.py</files>
  <action>Compute **P(w(i,j))** across **all directed routes in the snapshot** (same percentile rule as nodes). **route_criticality_score** = 0.70 * P(weight) + 0.30 * cross_community_component where cross component uses 0–100 flag as spec. Document exact formula in code comment referencing §7.10.</action>
  <verify>Synthetic edges: higher weight → higher P(weight); cross-community boosts score vs same-community at equal weight.</verify>
  <done>§7.10 implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Write route_metrics.csv and sanity tests</name>
  <files>src/metrics/run_metrics.py, tests/test_route_metrics.py</files>
  <action>Write **`route_metrics.csv`** with columns §6.6 order. Extend CLI so full metrics build ends with all three artifacts. Add test: column contract; **correlation smoke**: route_criticality_score not perfectly correlated with raw flight_count/analysis_weight (Pearson |ρ| &lt; 0.99 or document exception if graph is degenerate).</action>
  <verify>`pytest tests/test_route_metrics.py -q` passes.</verify>
  <done>METR-06 complete.</done>
</task>

</tasks>

<verification>
pytest; CSV written; sanity check logged in SUMMARY.
</verification>

<success_criteria>
route_metrics.csv reproducible from edges + community labels.
</success_criteria>

<output>
After completion, create `.planning/phases/02-graph-metrics-communities/02-05-SUMMARY.md`
</output>
