---
phase: 02-graph-metrics-communities
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/metrics/hub_bridge.py
  - src/metrics/tables.py
  - src/metrics/run_metrics.py
  - tests/test_hub_bridge_metrics.py
autonomous: true
must_haves:
  truths:
    - "Hub Score and Bridge Score match §7.6–§7.7 using P(s_total), P(PageRank), P(deg_total), P(betweenness)."
    - "metrics.csv exists with §6.4 columns; vulnerability_score and leiden_community_id are placeholders pending later plans (NaN / sentinel documented)."
    - "Graph-derived strength/degree align with nodes.csv within documented tolerance (METR-02 QA)."
  artifacts:
    - path: "data/processed/{snapshot_id}/metrics.csv"
      provides: "Per-airport metrics table (pre-community columns filled)"
    - path: "src/metrics/hub_bridge.py"
      provides: "Hub and bridge formulas"
  key_links:
    - from: "hub_bridge.py"
      to: "percentile.py + centralities.py + nodes.csv"
      via: "merge s_total, deg_total from nodes; centralities from graph"
---

<objective>
Implement **METR-04** (Hub / Bridge) and **METR-02** validation (strength/degree consistency with `nodes.csv`); write the first **`metrics.csv`** cut with **placeholder** `leiden_community_id` and **`vulnerability_score`** (NaN — METR-07 in Phase 3).

Purpose: Locks composite node scores before layering Leiden and routes.  
Output: `hub_bridge.py`, `tables.py` (or equivalent assembler), partial `run_metrics.py` stage, `metrics.csv`, tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@organization/ATNA_MVP_Technical_Spec_and_Workflow.md
@.planning/phases/02-graph-metrics-communities/02-CONTEXT.md
@data/processed
@src/metrics/percentile.py
@src/metrics/centralities.py
@src/metrics/graph_builder.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hub and Bridge scores</name>
  <files>src/metrics/hub_bridge.py</files>
  <action>From merged per-airport frame: compute **HubScore** = 0.50*P(s_total) + 0.30*P(PageRank) + 0.20*P(deg_total); **BridgeScore** = P(betweenness). Use `nodes.csv` for **s_total** and **deg_total**; use centrality outputs for PageRank/betweenness. Match spec naming: columns **`hub_score`**, **`bridge_score`**.</action>
  <verify>Unit test on synthetic data: known ordering constraints (e.g. if betweenness dominates, bridge P tracks it).</verify>
  <done>§7.6–§7.7 formulas implemented.</done>
</task>

<task type="auto">
  <name>Task 2: METR-02 consistency check</name>
  <files>src/metrics/tables.py</files>
  <action>Recompute **strength_in/out/total** and **degree_in/out/total** from the DiGraph (sum of weights / neighbor counts) and **assert close** to `nodes.csv` (rtol/atol or exact equality for integer degrees). On mismatch, fail fast in test or record in validation output — do not silently overwrite nodes.csv.</action>
  <verify>Test passes for MVP snapshot when `nodes.csv` exists; documents tolerance in assertion message.</verify>
  <done>METR-02 cross-check automated.</done>
</task>

<task type="auto">
  <name>Task 3: Write metrics.csv (placeholders)</name>
  <files>src/metrics/run_metrics.py, tests/test_hub_bridge_metrics.py</files>
  <action>Add orchestration function or CLI sub-step that writes **`metrics.csv`** with columns in **§6.4 order**: snapshot_id, airport_id, pagerank, betweenness, eigenvector, hub_score, bridge_score, vulnerability_score (NaN), leiden_community_id (use **-1** or NaN sentinel documented in validation notes until plan 04). Include **all airports** in nodes (or spec-defined universe). Wire `run_metrics.py` to run: load config → graph → centralities → hub/bridge → write CSV.</action>
  <verify>`pytest tests/test_hub_bridge_metrics.py -q` passes; test asserts required column names and dtypes sensible; file written under processed dir.</verify>
  <done>metrics.csv baseline on disk for MVP snapshot.</done>
</task>

</tasks>

<verification>
pytest + CSV exists with §6.4 headers; nodes/graph consistency test green.
</verification>

<success_criteria>
METR-02 QA and METR-04 delivered; metrics.csv ready for community merge.
</success_criteria>

<output>
After completion, create `.planning/phases/02-graph-metrics-communities/02-03-SUMMARY.md`
</output>
