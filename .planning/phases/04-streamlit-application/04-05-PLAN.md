---
phase: 04-streamlit-application
plan: 05
type: execute
wave: 4
depends_on: [04-02, 04-03, 04-04]
files_modified:
  - src/app/scenario_service.py
  - src/app/pages/scenario_editor.py
  - tests/test_streamlit_app_smoke.py
autonomous: false
must_haves:
  truths:
    - "Scenario editor runs the canonical engine from user inputs and shows before/after + ripple + affected-airport outputs."
    - "All seven pages load without uncaught exceptions under automated smoke coverage."
    - "Narrow/empty filters and tooltip states do not break page rendering."
  artifacts:
    - path: "src/app/scenario_service.py"
      provides: "adapter from UI payloads to scenarios.engine.run_scenario() contract"
    - path: "src/app/pages/scenario_editor.py"
      provides: "APP-06 interactive scenario workflow with form submit and result panes"
    - path: "tests/test_streamlit_app_smoke.py"
      provides: "AppTest smoke assertions for APP-01..APP-07 and APP-09 resilience"
  key_links:
    - from: "src/app/pages/scenario_editor.py"
      to: "src/scenarios/engine.py"
      via: "scenario_service invoking run_scenario"
      pattern: "run_scenario\\("
    - from: "tests/test_streamlit_app_smoke.py"
      to: "src/app/streamlit_app.py"
      via: "AppTest launch + per-page navigation assertions"
      pattern: "AppTest"
---

<objective>
Finish APP-06 and APP-09 by wiring live scenario execution into the app and adding smoke tests that prove page stability across the full seven-page experience.

Purpose: close the highest-risk interaction path (scenario editing) and verify full app robustness before Phase 5 QA/demo work.
Output: scenario service + scenario editor implementation + automated Streamlit AppTest smoke suite, followed by human product verification.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streamlit-application/04-RESEARCH.md
@.planning/phases/03-scenario-engine-vulnerability-integration/03-03-SUMMARY.md
@src/scenarios/engine.py
@data/processed/2025-12/scenarios.csv
@data/processed/2025-12/scenario_exposure.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement scenario service adapter and form-based execution flow</name>
  <files>src/app/scenario_service.py, src/app/pages/scenario_editor.py</files>
  <action>Create `scenario_service.py` that validates UI inputs and delegates scenario execution to `scenarios.engine.run_scenario()` using baseline graph + snapshot_id. In `scenario_editor.py`, implement `st.form` controls for airport removal and route removal payloads, execute only on submit, then render before/after score cards, ripple/affected airport outputs, and a scenario result table. Preserve deterministic behavior and avoid re-implementing scenario formulas in UI.</action>
  <verify>Run app, submit one airport-removal and one route-removal scenario, and confirm result sections populate with no exceptions.</verify>
  <done>Scenario editor performs canonical live runs and displays required APP-06 outputs end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Add Streamlit AppTest smoke coverage for all pages and empty-state resilience</name>
  <files>tests/test_streamlit_app_smoke.py</files>
  <action>Add AppTest-based tests that launch `src/app/streamlit_app.py`, navigate across all seven pages, and assert no uncaught exceptions. Include cases that simulate restrictive filters/empty datasets where feasible and confirm page logic renders fallback states instead of crashing. Keep tests fast and deterministic for CI/local runs.</action>
  <verify>Run `python -m pytest tests/test_streamlit_app_smoke.py -q` and confirm tests pass.</verify>
  <done>Automated smoke tests cover APP-01..APP-07 and APP-09 stability expectations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full seven-page Streamlit app with live scenario editor and automated smoke coverage.</what-built>
  <how-to-verify>
1) Launch: `python -m streamlit run src/app/streamlit_app.py`.
2) Visit each page (Overview, Network map, Airport explorer, Communities, Route explorer, Scenario editor, Methodology).
3) In Scenario editor, run one airport-removal and one route-removal scenario and verify before/after cards plus affected-airport outputs update.
4) On map/table pages, apply narrow filters to force sparse/empty results and confirm pages show fallback messages without tracebacks.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 4 execution readiness, or list defects to fix.</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_streamlit_app_smoke.py -q`
- `python -m streamlit run src/app/streamlit_app.py --server.headless true`
</verification>

<success_criteria>
- APP-06 scenario workflow is fully interactive and engine-backed.
- APP-08 and APP-09 are validated by loader architecture + smoke tests.
- Human review confirms seven-page product behavior is demo-ready.
</success_criteria>

<output>
After completion, create `.planning/phases/04-streamlit-application/04-05-SUMMARY.md`
</output>
