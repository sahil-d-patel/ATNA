---
phase: 04-streamlit-application
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/app/pages/overview.py
  - src/app/pages/network_map.py
  - src/app/pages/airport_explorer.py
autonomous: true
must_haves:
  truths:
    - "Overview page shows real snapshot KPIs and top-hub tables."
    - "Network map page renders airports/routes from artifacts and survives empty filters."
    - "Airport explorer supports sorting/filtering and exposes vulnerability with drilldown details."
  artifacts:
    - path: "src/app/pages/overview.py"
      provides: "APP-01 metrics, snapshot summary, top hubs view"
    - path: "src/app/pages/network_map.py"
      provides: "APP-02 map/filter interactions over precomputed artifacts"
    - path: "src/app/pages/airport_explorer.py"
      provides: "APP-03 sortable airport table and airport-specific detail pane"
  key_links:
    - from: "src/app/pages/overview.py"
      to: "src/app/data_loader.py"
      via: "load_metrics/load_nodes usage"
      pattern: "load_"
    - from: "src/app/pages/network_map.py"
      to: "src/app/ui/components.py"
      via: "empty-state guard before plotting"
      pattern: "empty|no rows"
    - from: "src/app/pages/airport_explorer.py"
      to: "metrics.csv.vulnerability_score"
      via: "table columns + detail cards"
      pattern: "vulnerability_score"
---

<objective>
Implement APP-01, APP-02, and APP-03 as production page modules using centralized loaders and robust filter handling.

Purpose: deliver the core exploratory product surface for network state and airport-level risk.
Output: completed Overview, Network map, and Airport explorer pages with real artifact data and safe empty-state behavior.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streamlit-application/04-RESEARCH.md
@.planning/phases/03-scenario-engine-vulnerability-integration/03-04-SUMMARY.md
@data/processed/2025-12/metrics.csv
@data/processed/2025-12/nodes.csv
@data/processed/2025-12/edges.csv
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Overview page with artifact-backed KPIs and rankings</name>
  <files>src/app/pages/overview.py</files>
  <action>Implement APP-01: load snapshot artifacts via shared loader, compute and display network totals (airport count, route count, aggregate flights/weights where available), show top hubs/bridges/vulnerability rankings, and include snapshot summary text. Use shared formatting/components for card and table rendering. Do not re-read CSVs directly in page code.</action>
  <verify>Run app and navigate to Overview; confirm all widgets/tables populate from current snapshot without exceptions.</verify>
  <done>Overview displays real KPIs and rankings with no placeholder-only content.</done>
</task>

<task type="auto">
  <name>Task 2: Build Network map page with threshold and month-safe filters</name>
  <files>src/app/pages/network_map.py</files>
  <action>Implement APP-02: render airport and route visualization (Plotly-based) using precomputed nodes/edges with controls for snapshot/month context and route-threshold filtering by analysis weight. Add explicit guard path when filters produce zero rows (show info message and skip chart call). Add tooltip fields for route and airport context aligned to schema.</action>
  <verify>Exercise low/high thresholds and edge cases where filters empty the dataset; confirm no chart exceptions and clear fallback messaging.</verify>
  <done>Network map behaves interactively with robust filter handling and informative tooltips.</done>
</task>

<task type="auto">
  <name>Task 3: Build Airport explorer with sortable grid and drilldown</name>
  <files>src/app/pages/airport_explorer.py</files>
  <action>Implement APP-03: create sortable/filterable airport table including hub_score, bridge_score, vulnerability_score, and selected metadata; provide drilldown panel for selected airport with supporting context (community, centrality, traffic proxies). Handle empty query results gracefully and preserve deterministic sort defaults.</action>
  <verify>Open Airport explorer, sort by vulnerability_score descending, and inspect at least one airport drilldown without runtime errors.</verify>
  <done>Airport explorer supports stable sort/filter flows and exposes vulnerability-integrated details.</done>
</task>

</tasks>

<verification>
- `python -m streamlit run src/app/streamlit_app.py --server.headless true`
- Manual smoke: Overview, Network map, Airport explorer pages load and interact without tracebacks.
</verification>

<success_criteria>
- APP-01, APP-02, APP-03 are implemented against artifact loaders only.
- Empty filter states do not crash charts/tables.
- Vulnerability score is visible and explorable in airport-facing UI.
</success_criteria>

<output>
After completion, create `.planning/phases/04-streamlit-application/04-03-SUMMARY.md`
</output>
