---
phase: 04-streamlit-application
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/config.py
  - src/app/data_loader.py
  - src/app/ui/components.py
  - src/app/ui/formatters.py
autonomous: true
must_haves:
  truths:
    - "All Streamlit pages read one canonical snapshot/artifact path contract."
    - "Missing or malformed artifact schemas fail with explicit errors instead of silent UI drift."
    - "Shared UI rendering primitives prevent empty-filter crashes across pages."
  artifacts:
    - path: "src/app/config.py"
      provides: "snapshot_id and artifact path resolution from config/atna.yaml"
    - path: "src/app/data_loader.py"
      provides: "cached typed artifact loaders and schema guards"
    - path: "src/app/ui/components.py"
      provides: "shared empty-state-safe table/card helpers"
    - path: "src/app/ui/formatters.py"
      provides: "shared display formatting helpers"
  key_links:
    - from: "src/app/data_loader.py"
      to: "config/atna.yaml"
      via: "config resolver + path expansion"
      pattern: "snapshot_id|processed"
    - from: "src/app/pages/*.py"
      to: "src/app/ui/components.py"
      via: "page rendering through shared helpers"
      pattern: "no rows|empty"
---

<objective>
Establish the Phase 4 core infrastructure: canonical config, cached artifact loaders, and shared UI helper primitives.

Purpose: keep page implementation plans small and consistent by locking data contracts and UI guardrails first.
Output: reusable config/loading/ui modules consumed by router and page plans.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streamlit-application/04-RESEARCH.md
@organization/ATNA_MVP_Technical_Spec_and_Workflow.md
@config/atna.yaml
@src/etl/config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement canonical app config and artifact path resolution</name>
  <files>src/app/config.py</files>
  <action>Implement app config loader resolving snapshot and artifact paths from `config/atna.yaml` (metrics, communities, route_metrics, scenarios, scenario_exposure, and baseline edges/nodes). Ensure deterministic resolution rooted at repo paths and clear error messages for missing config keys.</action>
  <verify>Run a short Python check that imports config resolver and prints resolved paths for current snapshot_id.</verify>
  <done>Config module provides stable snapshot-scoped artifact path resolution for all required files.</done>
</task>

<task type="auto">
  <name>Task 2: Implement cached artifact loaders with schema guards</name>
  <files>src/app/data_loader.py</files>
  <action>Implement `st.cache_data` loaders keyed by snapshot_id and resolved path, with required-column validation for each artifact contract (including `vulnerability_score`). Fail fast with descriptive `ValueError` on missing columns or unreadable files; do not silently continue.</action>
  <verify>Run a Python check that imports loaders and attempts to read each artifact for current snapshot_id.</verify>
  <done>All required artifact loaders return snapshot-filtered DataFrames and raise explicit errors on schema mismatch.</done>
</task>

<task type="auto">
  <name>Task 3: Add shared UI primitives for empty-state-safe rendering</name>
  <files>src/app/ui/components.py, src/app/ui/formatters.py</files>
  <action>Create reusable helpers for metric cards, dataframe/table rendering, and empty-filter guards used by downstream pages (APP-09). Include concise formatting helpers for percentages/scores and consistent "no rows for current filters" messaging. Keep helpers presentation-only; no scenario/math recomputation.</action>
  <verify>Import helpers in Python and run smoke calls for both non-empty and empty DataFrame inputs.</verify>
  <done>Shared helpers exist and can be reused by page plans to prevent chart/table crashes on empty slices.</done>
</task>

</tasks>

<verification>
- `python -c "from app.config import load_app_config; print(load_app_config())"`
- `python -c "from app.data_loader import load_metrics,load_communities,load_route_metrics,load_scenarios,load_scenario_exposure; print('ok')"`
</verification>

<success_criteria>
- Canonical config/loader layer exists and is snapshot-aware.
- Artifact schema validation is explicit and fail-fast.
- Empty-state-safe shared UI primitives are available for all page plans.
</success_criteria>

<output>
After completion, create `.planning/phases/04-streamlit-application/04-01-SUMMARY.md`
</output>
