---
phase: 04-streamlit-application
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/app/streamlit_app.py
  - src/app/pages/overview.py
  - src/app/pages/network_map.py
  - src/app/pages/airport_explorer.py
  - src/app/pages/communities.py
  - src/app/pages/route_explorer.py
  - src/app/pages/scenario_editor.py
  - src/app/pages/methodology.py
autonomous: true
must_haves:
  truths:
    - "App launches with a deterministic seven-page navigation shell."
    - "Every router target exists as an importable page module before feature work begins."
    - "The routing contract is explicit and stable, not reliant on Streamlit auto-discovery."
  artifacts:
    - path: "src/app/streamlit_app.py"
      provides: "st.Page + st.navigation registration for seven pages"
    - path: "src/app/pages/*.py"
      provides: "minimal callable scaffolds for all seven pages"
  key_links:
    - from: "src/app/streamlit_app.py"
      to: "src/app/pages/*.py"
      via: "st.Page file-path registration and pg.run()"
      pattern: "st\\.Page\\("
---

<objective>
Create the Phase 4 app shell and deterministic seven-page router using explicit page module registration.

Purpose: isolate routing scaffolding from feature implementation so page plans stay focused and smaller in scope.
Output: `streamlit_app.py` plus seven page scaffolds wired into `st.navigation`.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-streamlit-application/04-RESEARCH.md
@organization/ATNA_MVP_Technical_Spec_and_Workflow.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build explicit multipage router entrypoint</name>
  <files>src/app/streamlit_app.py</files>
  <action>Create `streamlit_app.py` using `st.Page` + `st.navigation`, set page config, and register exactly seven page modules mapped to APP-01..APP-07 names. Keep routing explicit in one file; do not use implicit `pages/` directory auto-discovery.</action>
  <verify>Run `python -m streamlit run src/app/streamlit_app.py --server.headless true` and confirm app starts without import/path errors.</verify>
  <done>App boots with a visible seven-page navigation menu and deterministic route ordering.</done>
</task>

<task type="auto">
  <name>Task 2: Create minimal callable scaffolds for all seven pages</name>
  <files>src/app/pages/overview.py, src/app/pages/network_map.py, src/app/pages/airport_explorer.py, src/app/pages/communities.py, src/app/pages/route_explorer.py, src/app/pages/scenario_editor.py, src/app/pages/methodology.py</files>
  <action>Create each page module with a callable page function (title + safe placeholder body) so all router targets exist and import cleanly. Keep scaffolds intentionally minimal and free of business logic; later plans will replace placeholder content with real artifact-backed views.</action>
  <verify>Launch the app and click through all seven pages; confirm no missing-file/import exceptions occur.</verify>
  <done>All seven pages render callable placeholders and are routable through the app shell.</done>
</task>

</tasks>

<verification>
- `python -m streamlit run src/app/streamlit_app.py --server.headless true`
- Manual smoke: all seven pages are visible and navigable without tracebacks.
</verification>

<success_criteria>
- Seven-page router is explicit and launchable.
- Every registered page file exists and is callable.
- Downstream page plans can focus on feature logic instead of router/scaffold setup.
</success_criteria>

<output>
After completion, create `.planning/phases/04-streamlit-application/04-02-SUMMARY.md`
</output>
